<% if @command == "go" %>
$("#update_<%= @schedule.id %>").fadeIn("slow").fadeOut("slow")
<% else %>
$("#row_<%= @schedule.id %>").fadeOut("slow")
<% end %>
var i_day = <%= Project.total_time("day",current_user) %>
var i_week = <%= Project.total_time("week",current_user) %>
timers(<%= current_user.working? %>,i_day,i_week);
