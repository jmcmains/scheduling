$("#hours_popup").data('day',<%= Schedule.total_time("day",current_user) %>);
$("#hours_popup").data('week',<%= Schedule.total_time("week",current_user) %>);
$("#hours_popup").data('working',<%= current_user.working? %>);
schedTimer();
update_sched();
$("#row_<%= @schedule.id %>").fadeOut("slow")
