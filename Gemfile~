source 'https://rubygems.org'

ruby '2.0.0'
gem 'rails', '4.0.0'

gem 'rails'

gem 'will_paginate'

group :development do
	gem 'sqlite3'
end

gem 'bootstrap-sass'
gem 'font-awesome-sass-rails'
gem 'masonry-rails'
gem 'bcrypt-ruby'
gem 'google_drive'

group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
  gem 'uglifier'
end

gem 'jquery-rails'

group :production do
  gem 'pg'
end

